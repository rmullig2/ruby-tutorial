version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user
hooks:
  BeforeInstall:
    - location: "bundle install"
      timeout: 300
      runas: ec2-user   
  ApplicationStart:
    - location: "ruby hello_app.rb"
      timeout: 300
      runas: ec2-user

