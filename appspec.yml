version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user/tutorial
permissions:
  - object: /home/ec2-users/tutorial
    owner: ec2-user
    group: ec2-user
hooks:
  ApplicationStart:
    - location: scripts/start_server
      timeout: 300

